/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
  //   // domains: ['evolveyoursuccess.com']
  },
  experimental: {
    appDir: true,
  },
}

module.exports = nextConfig
